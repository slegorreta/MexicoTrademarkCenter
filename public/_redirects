# Serve static files directly — never rewrite to index.html
/sitemap.xml  /sitemap.xml  200
/robots.txt   /robots.txt   200

# Pass API routes through to serverless functions — do NOT rewrite
/api/*  /api/:splat  200

# SPA catch-all — must be last
/*  /index.html  200
